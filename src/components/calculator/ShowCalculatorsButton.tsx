import GameButton from '../common/styled/GameButton';

function ShowCalculatorsButton() {
  return (
    <GameButton size="large">
      Show calculators
    </GameButton>
  );
}

export default ShowCalculatorsButton;
